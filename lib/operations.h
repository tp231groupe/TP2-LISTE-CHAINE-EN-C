#ifndef OPERATIONS_H
#define OPERATIONS_H
int linearSearch(int* array,int aizeA, int element);
int sortStatus(int* array, int size);
int* reverseArray(int* array, int size);
int* vectorProduct(int* arrayA, int* arrayB, int sizeA, int sizeB); 
#endif